# Changelog

## 0.0.1

- Initial release: `/open?path=…&line=…&column=…` URI handler that opens local
  files in any window, serving them through the `local-file:` scheme when the
  window is connected to a remote host.
